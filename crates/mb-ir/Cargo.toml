[package]
name = "mb-ir"
version.workspace = true
edition.workspace = true
description = "Core IR types for masterblaster tracker"

[features]
default = ["std"]
std = []

[dependencies]
arrayvec = { workspace = true }
libm = { workspace = true }
